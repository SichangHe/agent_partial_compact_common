Current-session message IDs for `partial_compact` range selection.

Use the visible IDs listed below as `from_message_id` and `to_message_id` endpoints when calling `partial_compact`. Some implementations list only `msg...` IDs; implementations that expose compacted visible ranges may also list `cmp...` IDs.

{{MESSAGE_ID_LINES}}
